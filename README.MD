#Neural Prophet Yahoo Finance Implementation

##Requirements
```shell
npm install -g http-server
pip install -r requirements.txt
```

##Usage
```shell
http-server -o --cors -p 3000
python3 main.py {ticker}
```

##Example
```shell
python3 main.py TSLA
```